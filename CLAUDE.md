# Working in this repository

See [AGENTS.md](AGENTS.md). It is the single contributor contract for this
repository and applies to every agent equally.

This file exists only because Claude Code reads `CLAUDE.md` and Codex reads
`AGENTS.md`. Keeping it as a pointer rather than a copy is the same rule the
repository itself enforces: one source, thin adapters, no second copy to drift.
